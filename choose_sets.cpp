#include <bits/stdc++.h>
using namespace std;

int main()
{
    int cnt[33554449];
    int n;
    while(cin>>n)
    {
        int po = (1<<n);
        for(int i=1; i<po; i++)
        {
            int temp = i;
            int bit = 0;
            while(temp>0)
            {
                if(temp%2)
                {
                    mul *=
                }
            }
        }
    }
}
